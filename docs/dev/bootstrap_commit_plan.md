# Bootstrap Commit Plan

status: active
owner: docs/dev
last_reviewed: 2026-08-05

The supplied Git bundle preserves this sequence:

1. `chore(repo): install agent-kit baseline`
2. `docs(governance): establish Blu Core authority and roles`
3. `chore(kernel): import CTS v0.22.0 golden runtime`
4. `docs(bootstrap): record source authority and validation receipts`

Future work begins only after the bootstrap validates cleanly.
